/**
 * Outline panel — lecture structure with timestamp anchors.
 *
 * Purpose: Will render hierarchical outline items from the Content Agent with monospace
 * timestamps and purple hover accents per UI spec (no card chrome).
 *
 * Agents: Consumes structured outline JSON derived from Transcript + Content agents.
 */

export function OutlineView(): JSX.Element {
  /** Reserved — implemented when study dashboard ships. */
  return <></>;
}
