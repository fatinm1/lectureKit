/**
 * Semantic search panel — query → timestamped transcript moments.
 *
 * Purpose: Accepts natural language questions and lists Chroma hits with staggered fade-in.
 *
 * Agents: Uses embeddings + retrieval from the Search Agent atop Transcript chunks.
 */

export function SearchView(): JSX.Element {
  /** Reserved — implemented when study dashboard ships. */
  return <></>;
}
