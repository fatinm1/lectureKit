/**
 * Summary panel — tri-depth summaries (90s / 5 min / full).
 *
 * Purpose: Presents typographic hierarchy with uppercase eyebrow labels and hairline dividers.
 *
 * Agents: Displays Content Agent summary blocks; bilingual toggle will re-fetch translations later.
 */

export function SummaryView(): JSX.Element {
  /** Reserved — implemented when study dashboard ships. */
  return <></>;
}
